<svg width="1200" height="630" viewBox="0 0 1200 630" xmlns="http://www.w3.org/2000/svg">
  <!-- Background -->
  <rect width="1200" height="630" fill="#1a1a1a"/>

  <!-- Gradient overlay -->
  <defs>
    <linearGradient id="grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#d4a574;stop-opacity:0.1" />
      <stop offset="100%" style="stop-color:#e34234;stop-opacity:0.1" />
    </linearGradient>
  </defs>
  <rect width="1200" height="630" fill="url(#grad)"/>

  <!-- Main text -->
  <text x="600" y="280" font-family="system-ui, -apple-system, sans-serif" font-size="80" font-weight="700" fill="#e8e6e3" text-anchor="middle">
    Speck
  </text>

  <!-- Subtitle -->
  <text x="600" y="350" font-family="system-ui, -apple-system, sans-serif" font-size="32" fill="#d4a574" text-anchor="middle">
    Opinionated feature specs for Claude Code
  </text>

  <!-- Tech stack -->
  <text x="600" y="420" font-family="system-ui, -apple-system, sans-serif" font-size="24" fill="#a8a8a8" text-anchor="middle">
    Bun • TypeScript • Claude-native
  </text>

  <!-- URL -->
  <text x="600" y="520" font-family="system-ui, -apple-system, sans-serif" font-size="28" fill="#d4a574" text-anchor="middle">
    speck.dev
  </text>
</svg>
